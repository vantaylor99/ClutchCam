description: Completed Faster-Whisper healthcheck Python binary fix
prereq: local-linux-live-transcription-opt-in
files: ai-stream-director/docker-compose.yml, ai-stream-director/tests/test_linux_compose_stack.py
----
The optional `faster-whisper` Compose service now uses `python3` for its socket
healthcheck. The stock `fedirz/faster-whisper-server:latest-cpu` image exposes
`python3`, not `python`, so the API could serve requests successfully while
Docker still reported the container as unhealthy.

Validation:

- `python -m unittest tests.test_linux_compose_stack -v`
  - `9` tests passed.
- `git diff --check`
  - passed with line-ending normalization warnings only.
