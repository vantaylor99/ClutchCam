description: Completed Python bytecode artifact cleanup
prereq: 
files: .gitignore, ai-stream-director/src/__pycache__/, ai-stream-director/tests/__pycache__/
----
Generated Python bytecode files are no longer tracked by Git.

Built:

- Kept `__pycache__/` and `*.pyc` ignore rules in `.gitignore`.
- Removed a duplicate `ai-stream-director/tmp*/` ignore entry while touching
  the ignore file.
- Removed previously tracked `*.pyc` artifacts from Git with `git rm --cached`,
  leaving source files and local generated bytecode behavior untouched.

Validation:

```powershell
cd ai-stream-director
& 'C:\Users\jacob\.cache\codex-runtimes\codex-primary-runtime\dependencies\python\python.exe' -m unittest discover -s tests -v
git status --ignored --short -- ai-stream-director/src/__pycache__ ai-stream-director/tests/__pycache__
```

Result:

- Full Python unit suite: 72 passed.
- Regenerated bytecode appears under ignored `__pycache__/` directories instead
  of as modified tracked files.

Note:

- The repo-local `.venv` currently points at the Windows Store Python package
  path and was not executable from the sandbox, so validation used the bundled
  Codex Python runtime.
