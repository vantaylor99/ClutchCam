description: Review the new local speech-pause transcription mode before it is treated as finished.
prereq: streaming-transcription-source-boundary
files: ai-stream-director/src/config.py, ai-stream-director/src/services/transcription.py, ai-stream-director/src/services/transcription_runtime.py, ai-stream-director/src/transcription_worker.py, ai-stream-director/tests/test_transcription_audio_extraction.py, ai-stream-director/tests/test_transcription_event_api.py, ai-stream-director/tests/test_transcription_runtime.py, ai-stream-director/tests/test_transcription_worker_entrypoint.py, ai-stream-director/tests/test_runtime_healthcheck_entrypoints.py, ai-stream-director/.env.example
difficulty: hard
----
Implemented `TRANSCRIPTION_SOURCE_MODE=vad-utterance` as an opt-in local voice activity detection source.

What changed:

- Added VAD configuration fields and environment examples for frame length, energy threshold, minimum speech, minimum silence, leading/trailing padding, and maximum utterance duration.
- Added `VadUtteranceAudioWindowDiscovery`, which wraps completed WAV chunk discovery, reads mono `pcm_s16le` WAV frames with Python's `wave` module, assembles bounded utterance WAV request files under `_vad/<stream_id>/`, and emits `AudioInputRef` values for the existing transcriber adapters.
- Kept `chunked` as the default source mode and as the fallback. VAD startup rejects non-WAV, non-mono, or non-`pcm_s16le` extraction settings.
- Added opt-in runtime pump behavior for VAD: provider partial events are rejected, and final events whose end timestamp is not newer than the last accepted final event for the same stream are suppressed by timestamp instead of text.
- Adjusted transcription response normalization so overlap windows still require timestamps, while VAD-style utterance windows whose `emit_from_seconds` equals their start can use duration fallback for text-only provider responses.

Validation run:

- `python -m unittest ai-stream-director.tests.test_transcription_audio_extraction ai-stream-director.tests.test_transcription_runtime ai-stream-director.tests.test_transcription_worker_entrypoint`
- `python -m unittest discover -s tests` from `ai-stream-director/`

## Review findings

- Checked the implement diff in `ddc5b8c`, the VAD state machine, source-mode construction, transcription normalization, runtime pump filtering, environment docs, and focused tests.
- Minor finding fixed inline: unreadable or empty VAD WAV chunks could leave active speech state alive across the bad chunk, and chunk-skip diagnostics depended on a caller-supplied logger. The review pass now resets VAD state on unreadable, missing-timestamp, or empty chunks, logs skipped chunks through the default module logger, and adds regression coverage for unreadable and empty chunks.
- No major findings were found. The simple root-mean-square threshold still needs real audio comparison before operators trust the mode live; that validation is explicitly covered by the next ticket, `vad-transcription-runtime-validation-docs`.

Review validation run:

- `python -m unittest ai-stream-director.tests.test_transcription_audio_extraction ai-stream-director.tests.test_transcription_event_api ai-stream-director.tests.test_transcription_runtime ai-stream-director.tests.test_transcription_worker_entrypoint`
- `python -m unittest discover -s tests` from `ai-stream-director/`
