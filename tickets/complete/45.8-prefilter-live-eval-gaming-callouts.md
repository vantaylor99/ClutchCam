description: The local transcript filter now recognizes common live gaming callouts so plausible moments reach the AI director.
files: ai-stream-director/src/services/ai.py, ai-stream-director/tests/test_service_boundaries.py, ai-stream-director/tests/test_runtime_event_pipeline.py
----
The transcript prefilter now accepts the live gaming callouts identified from
the medium Faster-Whisper evaluation without lowering the global transcript
length guard.

Completed behavior:

- Added default longer gaming callout phrases: "that was nasty", "behind you",
  "good work", "triple", and "legend".
- Added an exact-match short callout list for "help", so a lone "help" can
  trigger while short filler such as "ok" remains rejected.
- Gaming callout matches emit `Matched gaming callout phrase: <phrase>.`
- Duplicate suppression continues to suppress repeated matching phrases across
  nearby transcript events and streams, including "behind you" and exact "help".
- Runtime transcript event coverage proves an accepted "that was nasty" event
  reaches `ai_director.decide(...)` with a `candidate_signal`.

## Review findings

- Checked the implement diff from commit
  `7207d3cb575d1debe1f1af0d9055d5da199ccf93` and the touched source/tests.
- Checked the short-callout path: "help" is exact normalized text only, so it
  does not lower the general minimum transcript length rule for unrelated short
  filler.
- Checked duplicate handling for the new phrase classes: repeated matched
  phrases are still suppressed by normalized phrase identity across recent
  transcript history.
- Checked the runtime event path: accepted callout events still route through
  the existing router, local prefilter, AI director call, and candidate signal
  plumbing.
- No minor findings were found, so no inline source changes were made.
- No major findings were found, so no follow-up fix, plan, or backlog ticket was
  created.
- No tripwires were recorded; the added phrases use the existing normalized
  substring matcher intentionally, and no conditional future concern stood out
  beyond the already-filed broader context-boundary backlog work.

Validation:

- `python -B -m unittest tests.test_service_boundaries tests.test_runtime_event_pipeline -v`
  passed: 36 tests.
- `python -B -m unittest discover -s tests -v` passed: 278 tests.
- No repo lint command was configured in the files checked, so no separate lint
  command was run.
