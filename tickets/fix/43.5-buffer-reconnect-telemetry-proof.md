description: Make buffer reconnect acceptance machine-verifiable
prereq: linux-generated-ingest-acceptance
files: ai-stream-director/src/services/buffer.py, ai-stream-director/scripts/compose_generated_ingest_checkpoint.py, ai-stream-director/scripts/smoke_buffer_worker.py, ai-stream-director/tests/test_buffer_supervision.py, ai-stream-director/tests/test_compose_generated_ingest_checkpoint.py, docs/runbooks/local-linux-compose.md
----
The Linux acceptance run proved useful reconnect behavior at the data plane:
after two bounded `player_1` generated publishers with a gap, the same
`buffer-worker` container remained alive and the latest segment sequence
advanced from `55` to `61`. However, the historical acceptance criterion also
required bounded logs to show `buffer_ffmpeg_exited stream=player_1` and a later
`buffer_ffmpeg_started stream=player_1`. Those exact events were not present in
the captured window.

Expected behavior:

- Reconnect acceptance should be machine-verifiable without relying on brittle
  assumptions about whether SRS closes an RTMP consumer process between
  publishers.
- The acceptance signal should prove that the worker remains alive, segment
  sequence advances after a second publish, and useful reconnect/recovery
  telemetry is emitted when an FFmpeg child actually exits or restarts.
- If process-exit logs are retained as a criterion, tests should make the
  logging contract explicit and the runbook should describe when those logs are
  expected.
- If segment advancement is the primary criterion, the runbook and checkpoint
  evidence requirements should say that clearly and avoid requiring nonexistent
  exit/restart logs.

Key evidence from the Linux run:

```text
/home/vantaylor99/clutchcam-test-evidence-20260627T002508Z
```

TODO:

- Reproduce the current reconnect evidence path with fixture/process-level tests
  where possible.
- Decide whether reconnect acceptance should require process restart logs or
  data-plane segment advancement.
- Update implementation, checkpoint helpers, and runbook text so the selected
  criterion is deterministic.
- Rerun the Linux reconnect acceptance and retain evidence.

